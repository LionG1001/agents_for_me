interface:
  display_name: "Rsync Relay Transfer"
  short_description: "Use the documented relay from remote Linux"
  default_prompt: "Use $rsync-relay-transfer to read my SiYuan relay note and safely perform the requested transfer from remote Linux."
